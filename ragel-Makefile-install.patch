diff -up ragel/Makefile.in.fedora ./ragel/Makefile.in
--- ragel/Makefile.in.fedora	2009-04-11 18:19:45.000000000 -0600
+++ ragel/Makefile.in	2009-04-11 18:20:04.000000000 -0600
@@ -109,6 +109,6 @@ clean:
 
 install: all
 	install -d $(DESTDIR)$(prefix)/bin
-	install -s ragel $(DESTDIR)$(prefix)/bin/ragel
+	install ragel $(DESTDIR)$(prefix)/bin/ragel
 
 -include $(DEPS)
